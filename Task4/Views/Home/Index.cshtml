@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @foreach (User user in ViewBag.Users)
    {
        <p>@user.Name @user.Email @user.RegisterDate @user.Status @user.LastLogin</p>
    }
</div>
